[pytest]
testpaths = tests
norecursedirs = _external .git build modelito.egg-info
asyncio_default_fixture_loop_scope = function
markers =
    smoke: quick smoke tests
    integration: slow integration tests
